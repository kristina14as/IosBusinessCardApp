# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Business Card App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Business Card App
  pod 'Firebase/Analytics'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'Firebase/Core'  

  target 'Business Card AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Business Card AppUITests' do
    # Pods for testing
  end

end
